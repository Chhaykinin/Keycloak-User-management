server:
  port: 9090
  error:
    include-stacktrace: never
spring:
  application:
    name: api-gateway
  config:
    import: optional:configserver:http://localhost:8888
management:
  tracing:
    sampling:
      probability: 1.0